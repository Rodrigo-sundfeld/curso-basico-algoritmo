algoritmo "ContadorDecrescente"
var
   contador: Inteiro
inicio
      contador <- 10
      Enquanto (contador >= 0) faca
               escrevaL(contador)
               contador <- contador - 1
      FimEnquanto
      escrevaL("Terminei de contar")
fimalgoritmo
