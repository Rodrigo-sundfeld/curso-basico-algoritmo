algoritmo "ContadorComRepita"

var
   contador: Real
inicio
      contador <- 1
      Repita
            EscrevaL(contador)
            contador <- contador + 1
      Ate (contador > 10)
fimalgoritmo
